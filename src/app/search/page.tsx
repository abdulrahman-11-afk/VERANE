import {getProducts} from '@/lib/products';import {ProductGrid} from '@/components/ProductGrid';export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const {q}=await searchParams;const result=getProducts({q});return <div className="container py-16"><p className="eyebrow">Search VÉRANE</p><form className="mt-5 flex max-w-xl border-b border-black pb-3"><input autoFocus name="q" defaultValue={q} placeholder="Search by name, category or fabric" className="w-full bg-transparent text-xl outline-none"/><button className="text-xs uppercase tracking-widest">Search</button></form><p className="mt-12 text-sm text-black/50">{q?`${result.length} results for “${q}”`:'Explore the collection'}</p><div className="mt-7">{result.length?<ProductGrid products={result}/>:<p className="py-20 text-center font-serif text-2xl">We couldn&apos;t find anything matching your search.</p>}</div></div>}
