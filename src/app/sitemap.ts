import type {MetadataRoute} from 'next';import {products} from '@/lib/products';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_APP_URL||'http://localhost:3000';return [{url:base,changeFrequency:'weekly',priority:1},{url:`${base}/shop`,changeFrequency:'daily',priority:.9},{url:`${base}/about`,priority:.6},{url:`${base}/journal`,changeFrequency:'weekly',priority:.6},...products.map(product=>({url:`${base}/product/${product.slug}`,changeFrequency:'weekly' as const,priority:.8}))]}
