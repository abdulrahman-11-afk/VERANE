'use client';
import {useState} from 'react';import {Product} from '@/lib/products';import {useCart} from './CartProvider';
export function AddToCart({product}:{product:Product}){const {add}=useCart();const [size,setSize]=useState(product.sizes[0]);const [color,setColor]=useState(product.colors[0]);const [added,setAdded]=useState(false);return <div className="space-y-7"><div><p className="eyebrow mb-3">Color</p><div className="flex gap-2">{product.colors.map(item=><button key={item} onClick={()=>setColor(item)} className={`border px-4 py-2 text-xs ${color===item?'border-black bg-black text-white':'border-black/20'}`}>{item}</button>)}</div></div><div><p className="eyebrow mb-3">Size</p><div className="flex gap-2">{product.sizes.map(item=><button key={item} onClick={()=>setSize(item)} className={`border px-4 py-2 text-xs ${size===item?'border-black bg-black text-white':'border-black/20'}`}>{item}</button>)}</div></div><button onClick={()=>{add(product,size,color);setAdded(true)}} className="w-full bg-[#181817] py-4 text-xs uppercase tracking-[.2em] text-white transition hover:bg-[#66715c]">{added?'Added to your bag':'Add to bag'}</button></div>}
